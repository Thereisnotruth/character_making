import React, { useState } from 'react';

import { Canvas, Controller } from '../components';

const Board = () => {
    return(
        <div className="board">
            <Canvas 
            />
            
            <Controller 
            />
           
        </div>
    );
};

export default Board;